{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "dev": {
      "cache": false,
      "dependsOn": ["^accelerate-cms-ui#build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "sb": {
      "outputs": []
    },
    "lint": {
      "outputs": []
    }
  }
}
